rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Requires Anonymous sign-in to be enabled in Firebase Authentication.
    match /stores/{storeId}/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
